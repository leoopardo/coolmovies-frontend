import { Box, Drawer } from '@mui/material';
import { memo } from 'react';

export const ViewListMovieRevies = memo(() => {
  return (
    <Box sx={{ minHeight: '40dvh', backgroundColor: 'transparent' }}></Box>
  );
});
